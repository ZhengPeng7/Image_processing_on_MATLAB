粗调:
1. 先调用fit_curve函数, 在其中, 对L0, Lf, sigma进行满足约束的遍历, 并输出图像;
2. 观察出现的图像, 当比较拟合的图像出现时, Ctrl+c使之停止, 查看图像的标号: figure(XX), 输入para_all(XX), 即可出现这幅图像对应的三个参数.

微调: 
1. 转入test的16行, 输入para_all()显示的三参, 然后进行手动微调.